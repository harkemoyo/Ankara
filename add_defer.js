const fs = require('fs'); const files = fs.readdirSync('.').filter(f => f.endsWith('.html')); let changes = 0; files.forEach(f => { let c = fs.readFileSync(f, 'utf8'); let o = c; c = c.replace(/<script src="assets\/global\.js"><\/script>/g, '<script src="assets/global.js" defer></script>'); if (o !== c) { fs.writeFileSync(f, c); console.log('Updated ' + f); changes++; } }); console.log('Total files updated: ' + changes);
